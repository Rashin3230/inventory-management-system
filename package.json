{
  "name": "inventory-management-system",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w inventory-backend\" \"npm run dev -w inventory-frontend\"",
    "dev:backend": "npm run dev -w inventory-backend",
    "dev:frontend": "npm run dev -w inventory-frontend",
    "build": "npm run build -w inventory-backend && npm run build -w inventory-frontend",
    "seed": "npm run seed -w inventory-backend",
    "lint": "npm run lint -w inventory-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
